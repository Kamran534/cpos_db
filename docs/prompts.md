# Prompts

- Build CSU server with Swagger, modular controllers/routes/services, auth (JWT, nodemailer), and sync module for two DBs (local + online).
- Sync options:
  - Call syncing APIs hourly (configurable CRON).
  - Socket-based auto update (bidirectional) when needed.
- Proper error handling everywhere.
- Add docs folder for detailed checklists and instructions.
